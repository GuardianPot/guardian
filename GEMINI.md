# Gemini CLI adapter

Use `AGENTS.md` as the canonical repository policy. Gemini CLI is a local or
sandboxed fallback/research tool and has no merge, repository-admin, release,
secret, or production-signing authority.
