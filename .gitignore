# Node and frontend output
node_modules/
dist/
*.tsbuildinfo
apps/web-console/vite.config.js
apps/web-console/vite.config.d.ts

# Go output
bin/
*.test

# Local environment and secrets
.env
.env.*
!.env.example

# Lab state
.lab/

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Created by any `go` command run against the workspace rather than with
# GOWORK=off. It is a local build artefact: every module pins its own
# dependencies in its own go.sum, and this file has been committed by accident
# twice.
go.work.sum
