---
id: P1-W1
phase: 1
title: Build the Control Plane service shell
status: approved-for-implementation
risk: high
components:
  - control-plane
  - database
decision_refs:
  - CP-01
  - CP-02
  - CP-03
  - CP-05
  - CP-06
  - DA-01
  - DA-02
  - DA-07
  - TS-01
  - TS-08
acceptance_refs:
  - Phase 1 P1-W1 acceptance
depends_on:
  - P1-G0
  - PHASE-0-GATE
allowed_paths:
  - "apps/control-plane/**"
  - "deploy/control-plane/**"
  - "tests/integration/control-plane/**"
  - "docs/runbooks/control-plane/**"
  - "docs/work-packages/phase-1/P1-W1.md"
  - "Taskfile.yml"
  - ".github/workflows/quality.yml"
forbidden_paths:
  - "apps/edge-agent/**"
  - "apps/web-console/**"
  - "proto/**"
requires_owner_review: true
requires_security_review: true
---

# P1-W1 — Build the Control Plane service shell

## 1. Purpose

Turn the Go Control Plane placeholder into the durable modular-monolith
foundation required by every other Phase 1 Control Plane package.

## 2. Why now

Authentication, environments, enrollment, health, audit, and jobs need shared
startup, configuration, transaction, migration, and module boundaries.

## 3. Inputs and decisions

- Go modular monolith using `net/http` and minimal middleware.
- PostgreSQL 18.x with explicit SQL and generated typed access.
- `pgx/v5`, `sqlc`, and `goose`, pinned to secure supported versions.
- OpenTelemetry-first instrumentation with no mandatory vendor backend.
- Development-only forward migration and reset/reseed recovery policy.

## 4. Dependencies

P1-G0 and Product Owner approval of the Phase 0 gate.

## 5. Scope

- Configuration and lifecycle for one Control Plane process.
- Module shells for auth, environment, devices, deception placeholder, health,
  audit, jobs/outbox, and API.
- PostgreSQL connection management, migrations, typed queries, and explicit
  transaction boundaries.
- Readiness/liveness endpoints and structured startup/shutdown behavior.
- Append-oriented audit substrate; P1-W10 owns the complete audit model.

## 6. Non-goals

- User login, environment behavior, enrollment, or device streaming.
- A second database, broker, cache, ORM, or microservice split.
- Production deployment sizing or high availability.

## 7. Allowed paths

Only the paths listed in frontmatter. Workflow changes are limited to the
approved PostgreSQL integration gate.

## 8. Security constraints

No credentials in source or logs. Database URLs and master material enter via
approved configuration boundaries. Health endpoints expose no secrets or raw
internal errors.

## 9. Implementation requirements

- Human-readable SQL migrations embedded or packaged reproducibly.
- No ORM auto-migration and no down migrations.
- Rollback documentation must say to revert code and reset/reseed the
  development database; do not add compatibility shims.
- Module code cannot directly reach another module's tables.
- Service restart must preserve committed state.

## 10. Required tests

- Unit tests for configuration, lifecycle, and transaction behavior.
- PostgreSQL 18 integration test from an empty database.
- Migration idempotency/version tests and deliberate migration failure test.
- Restart persistence test.
- `task validate` and container smoke build.

## 11. Acceptance criteria

- A fresh PostgreSQL database migrates successfully.
- The documented development reset/reseed strategy is reproducible.
- Restart preserves committed state and unfinished durable job metadata.
- Module boundaries and startup failure states are explicit.

## 12. Evidence required

Migration output, PostgreSQL integration logs, restart result, dependency and
license inventory, and CI links.

## 13. Stop and escalate

Stop for a new datastore/broker/runtime, ORM auto-migration, cross-module table
access, production secret, or any requirement for backward compatibility.

## 14. Deliverables

Control Plane foundation, migrations, generated query layer, integration tests,
runbook, and acceptance evidence.

## 15. Implementation evidence

Candidate evidence recorded on 2026-08-23 for owner and security review:

- `task validate` passed, including repository policy, formatting, contracts,
  Go vet/tests, web checks, container build/run, and PostgreSQL 18 integration.
- The PostgreSQL harness proved fresh and idempotent forward migration,
  deliberate migration rollback, no serve-time auto-migration, explicit
  transaction rollback, restart persistence, unfinished job durability,
  append-oriented audit insertion, sanitized health responses, secret-free
  logs, and SIGTERM shutdown.
- `go test -race -count=1 ./...` passed for the Control Plane and Edge baseline
  inside the digest-pinned Go 1.27 build image with CGO enabled.
- `govulncheck` 1.7.0 found no reachable vulnerability in the Control Plane,
  Edge baseline, or sqlc CLI. The goose CLI source graph has no reachable
  vulnerable call; its tool-only module still contains an uncalled advisory for
  the unmaintained `openpgp` package.
- Current selected versions in the runtime, sqlc tool, and goose tool module
  graphs have no Go module retraction. Module checksums verify successfully.
- `go-licenses` 2.0.1 accepted the runtime and tool graphs. Its goose scan
  required one documented module-root exception for `github.com/segmentio/asm`;
  the root license was manually verified as MIT No Attribution.
- The Distroless Debian 13 nonroot digest passed Cosign 3.1.3 keyless identity,
  certificate-authority, claims, and transparency-log verification.
- Trivy 0.74.0 reported zero high or critical findings in both the final Debian
  13 distroless filesystem and the compiled Guardian Go binary.
- Two clean `CGO_ENABLED=0`, `-trimpath`, stripped builds produced identical
  SHA-256 `cdac9bbaa431363e8f31a641a87fe1e6e342bd367d544c20f4878180fb97c813`.

Security impact: the service accepts credentials only through environment
configuration, emits no raw readiness errors, runs as UID/GID 65532 in a
shell-free image, and has a smoke-tested read-only/no-capabilities execution
profile. Module architecture tests reject direct data-layer imports.

Known limitations match package non-goals: module implementations, login,
enrollment, a running durable worker loop, production database roles, sizing,
and high availability remain owned by later packages. CI and PR links are
recorded in the linked GitHub review rather than embedded as mutable URLs here.
