---
id: P1-W2
phase: 1
title: Implement local user authentication
status: approved-for-implementation
risk: critical
components:
  - authentication
  - control-plane
decision_refs:
  - IA-01
  - IA-02
  - IA-03
  - IA-04
  - IA-05
  - IA-06
  - SA-11
  - SA-13
  - SA-14
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-06
  - Product Owner D8-A approval on 2026-08-22
  - Product Owner W2-C1-A through W2-C7-A approval on 2026-08-26
acceptance_refs:
  - Phase 1 P1-W2 acceptance
depends_on:
  - P1-W1
  - P1-W10
allowed_paths:
  - "apps/control-plane/go.mod"
  - "apps/control-plane/go.sum"
  - "apps/control-plane/internal/auth/**"
  - "apps/control-plane/internal/app/**"
  - "apps/control-plane/internal/config/**"
  - "apps/control-plane/cmd/**"
  - "apps/control-plane/internal/api/**"
  - "apps/control-plane/internal/storage/**"
  - "openapi/**"
  - "tests/integration/auth/**"
  - "security/**"
  - "docs/runbooks/auth/**"
  - "docs/work-packages/phase-1/P1-W2.md"
  - "Taskfile.yml"
forbidden_paths:
  - "apps/edge-agent/**"
  - "apps/web-console/**"
  - "proto/**"
requires_owner_review: true
requires_security_review: true
---

# P1-W2 — Implement local user authentication

## 1. Purpose

Provide secure local owner/operator authentication for the Phase 1 console and
API without a default password or client-stored bearer credential.

## 2. Why now

Every operator-facing Phase 1 action requires an authenticated actor and audit
identity before UI and domain workflows can be trusted.

## 3. Inputs and decisions

- Single-organization local account baseline; OIDC and role editor are out.
- Argon2id password hashing and server-side PostgreSQL sessions.
- CLI-generated, 15-minute, one-time bootstrap token stored only as a hash.
- TOTP for owner/admin plus ten hashed single-use recovery codes.
- 15-minute idle and 8-hour absolute session expiry.
- Synchronizer CSRF token and secure same-origin browser policy.

## 4. Dependencies

P1-W1 persistence/API shell and P1-W10 append-oriented audit API.

## 5. Scope

- One-time admin bootstrap command and API exchange.
- Password creation, verification, change, and forced session invalidation.
- TOTP enrollment/verification and recovery-code consumption.
- Login, logout, session lookup/revocation, and auth audit events.
- Persistent account/source throttling with configurable thresholds.

## 6. Non-goals

- OIDC/SSO, password reset email, fine-grained RBAC, API tokens, passkeys, or
  multi-organization user administration.

## 7. Allowed paths

Only the authentication, API contract, test, security, and runbook paths listed
in frontmatter.

## 8. Security constraints

- Session tokens, bootstrap tokens, recovery codes, passwords, and TOTP codes
  never appear in logs or plaintext database columns.
- TOTP seeds use the approved SecretStore encryption boundary.
- Cookie name uses the `__Host-` prefix with Secure, HttpOnly, SameSite=Strict,
  path `/`, and no Domain attribute.
- State-changing requests require CSRF and origin validation.

## 9. Implementation requirements

- Calibrate Argon2id on the supported runtime while meeting or exceeding the
  current approved security minimum; store parameters with each hash.
- Rotate session identity after login and privilege-sensitive changes.
- Enforce idle and absolute expiry on the server.
- Default throttle: five failed attempts in five minutes, followed by
  exponential delay capped at fifteen minutes; make values configuration.
- Recovery codes are displayed once and each can be consumed once.

## 10. Required tests

- Bootstrap replay, expiry, and concurrent-use tests.
- Argon2 parameter, malformed hash, and timing-safe comparison tests.
- TOTP enrollment, drift-window, replay, and recovery-code tests.
- Session fixation, idle/absolute expiry, logout, revocation, and cookie tests.
- CSRF/origin, brute-force throttle, and secret-redaction integration tests.

## 11. Acceptance criteria

- No default password exists and bootstrap cannot be reused.
- The browser holds only an opaque session cookie, never the session secret's
  server-side record or a localStorage token.
- TOTP is required for owner/admin login.
- Failed logins and security changes create audit entries without secrets.

## 12. Evidence required

Auth threat-test results, redacted cookie/header evidence, audit samples,
dependency review, and CI links.

## 13. Stop and escalate

Stop for external identity services, JWT/localStorage UI auth, plaintext
secrets, weaker MFA, recovery behavior outside this package, or security tests
that require disabling controls.

## 14. Deliverables

Local auth module, bootstrap CLI, API contract, migrations/queries, security
tests, and operator recovery runbook.

## 15. Approved execution contract — 2026-08-26

The Product Owner approved W2-C1-A through W2-C7-A as one package. The
implementation is constrained as follows.

- **W2-C1-A — narrow runtime/dependency/CI expansion:** `go.mod`, `go.sum`,
  `internal/app`, `internal/config`, and `Taskfile.yml` may change only for
  P1-W2 dependency pinning, auth composition/configuration, and the dedicated
  auth integration gate. The existing SecretStore implementation is reused
  without modification.
- **W2-C2-A — password and Argon2id profile:** use a pinned compatible secure
  `golang.org/x/crypto/argon2` release and strict PHC records. The floor is
  Argon2id with 64 MiB, three passes, four lanes, a 16-byte random salt, and a
  32-byte tag. Configuration may raise but not lower the floor. Passwords use
  NFC normalization, 12–128 Unicode code points, a byte bound, a local
  common/context-specific blocklist, and no composition rule. Runtime
  calibration evidence must remain below one second.
- **W2-C3-A — one-time owner bootstrap:** a CLI-created 32-random-byte
  Base64URL token is stored only as SHA-256, expires after 15 minutes, and
  invalidates older unconsumed bootstrap tokens. Atomic exchange creates the
  single Phase 1 owner in `pending_mfa`, returns the TOTP provisioning URI and
  ten recovery codes once, and creates no session. First valid password plus
  TOTP login activates the owner. Recovery cannot activate a pending owner;
  interrupted pre-activation recovery is development reset/reseed only.
- **W2-C4-A — MFA and recovery:** use a per-user encrypted 256-bit TOTP seed,
  HMAC-SHA-256, six digits, 30-second steps, a plus/minus one-step window, and
  a strictly increasing persisted accepted counter. Ten independent 128-bit
  recovery codes are hashed and atomically single-use. Recovery login revokes
  existing sessions before creating its replacement.
- **W2-C5-A — session/CSRF/origin:** generate independent 32-random-byte
  session and CSRF secrets and persist only hashes. The browser receives only
  `__Host-guardian_session` with Secure, HttpOnly, SameSite=Strict, Path `/`,
  no Domain, plus a no-store synchronizer CSRF token for memory-only use.
  Server-side expiry is 15 minutes idle and eight hours absolute. State
  changes require cookie, CSRF, and exact configured HTTPS Origin. Serve fails
  closed without an absolute owner-only master-key file and HTTPS public
  origin.
- **W2-C6-A — durable abuse/audit/API behavior:** persist account and source
  failure windows and exponential delay across restart: five failures in five
  minutes, capped at 15 minutes. Auth mutations append the approved P1-W10
  audit actions atomically without secrets. Requests are bounded and denials
  are generic and no-store, with the same external behavior for known and
  unknown accounts.
- **W2-C7-A — staged acceptance:** real PostgreSQL and HTTP/TLS fixtures own
  package evidence for bootstrap, cookies, CSRF/origin, MFA, recovery,
  sessions, concurrency, restart, audit, and redaction. P1-W11 retains actual
  browser screen, localStorage, and journey evidence; P1-W2 does not claim it
  through mocks.
