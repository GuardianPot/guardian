---
id: P2-W1
phase: 2
wave: capability
title: Routed presence driver
status: draft
risk: high
components:
  - edge-agent
decision_refs:
  - DC-12
  - SP-01
  - SP-02
acceptance_refs:
  - AC-ON-004
  - "P2-W1 acceptance: reboot/reconcile restores intended addresses"
  - "P2-W1 acceptance: failed deploy does not leave orphan IP"
depends_on:
  - P1-W8
  - P2-W15
blocks:
  - P2-W5
  - P2-W6
  - P2-W7
  - P2-W8
allowed_paths:
  - "apps/edge-agent/internal/presence/**"
  - "apps/edge-agent/internal/privileged/**"
  - "docs/work-packages/phase-2/**"
  - "security/**"
forbidden_paths:
  - "apps/web-console/**"
  - "openapi/**"
  - "0-planning-documents/**"
requires_owner_review: true
requires_security_review: true
---

# P2-W1 — Routed presence driver

## 1. Purpose

Put a decoy's address on the network, take it off again, and never be wrong
about which of those is true.

## 2. What this package is made of

Two halves with very different risk.

**The driver** decides what should happen: which addresses are wanted, whether
each may safely be taken, what to release, and what to report. It holds no
privilege and reaches the host only through a seam.

**The netlink adapter** is what actually adds and removes an address, and it
runs as root inside the `P1-W8` privileged helper. `AGENTS.md` names privileged
networking as a stop-and-ask; the owner authorised it, and section 7 records
what that authorisation actually cost.

Both halves are implemented. The conflict probe turned out to need neither root
nor a contract change, which is section 6.

## 3. Scope

1. `apps/edge-agent/internal/presence`: the reconciler, its `Driver`,
   `ConflictProbe`, and `Reservations` seams, and the reconciliation rules.
2. Reservation persistence, so a restarted agent can release an address a
   previous process applied.
3. Desired-versus-observed reporting per address.

## 4. Non-goals

- **No nftables.** `P2-W2` owns egress, and the agreed order puts it before any
  attacker-facing pack for that reason.
- **No container.** `P2-W3` owns the runtime; an address without a listener is
  still a correct outcome for this package.
- **No scanning.** The conflict probe answers one question about one address the
  operator already chose. It is not discovery, and nothing here enumerates a
  network.

## 5. The rules, and why they are these rules

**An address is taken only after something says it is free.** `AC-ON-004`
requires a deployment onto an address another host answers on to be refused
fail-safe with the existing host unaffected. The reconciler treats "the probe
could not tell me" as a refusal too, because it is not "the address is free".
Taking an address that turns out to belong to a production host is how a
deception product causes the outage it was bought to prevent.

**Removals run before applies.** The Control Plane permits an address to move
between decoys. Applying the new holder first would put a duplicate on the wire.

**A failed apply releases in the same pass.** The acceptance criterion is that a
failed deploy leaves no orphan IP. The window in which one exists is between a
partial apply and the next reconcile, so the release is issued there rather than
deferred.

**An unconfirmed apply is not presence.** A driver that will not say it applied
the address has not applied it. The same rule the decoy observed state follows:
absence of confirmation is never a claim.

**Reservations are persisted.** A restart is a new process with empty memory and
a host still holding every address the old one applied. Without the reservation
set, an address the Control Plane stopped asking for would stay on the interface
until the host went down.

## 6. The conflict probe needs no privilege

This was expected to be the package's hardest dependency. The obvious
implementation of "does another host already answer on this address" is an ARP
probe, which needs a raw socket, which needs root and a new operation on the
privileged helper's contract — a public-contract change and a
privileged-networking change at once.

It is not necessary. The kernel already knows how to ask the question: sending
one datagram to an on-link address makes it resolve the neighbour, and the
result lands in `/proc/net/arp`, which is world-readable. A complete entry with
a hardware address means something replied; an entry that never completed means
nothing did.

Verified on a live segment as uid 1000, with all capabilities dropped:

```
uid=1000: 172.17.0.1 in use, 172.17.99.234 free
```

`task presence:probe` reproduces it. `NeighbourProbe` is the production
implementation and the same run exercises it.

This is a pre-check on one address the operator already chose. It is not
discovery: one address per call, one datagram, nothing enumerated, and no code
path that walks a range or a port list.

On a host that cannot read a neighbour cache the probe reports that it could not
check, and the reconciler refuses — so a developer machine cannot claim an
address.

## 7. The netlink adapter, and what it cost

`EnsureAddress` was already validated, allowlisted, audited, and idempotent in
the helper; `P1-W8` built the boundary and left the implementation out. Filling
it needed no contract change — the RPC exists — but it needed two things that
are worth stating plainly.

**The service profile had to be widened.** The shipped unit ran root with an
*empty* capability bounding set, `PrivateNetwork=yes`, and
`RestrictAddressFamilies=AF_UNIX`. Under all three, netlink is not merely
unauthorised but impossible. The adapter is unreachable without exactly:

```
CapabilityBoundingSet=CAP_NET_ADMIN
PrivateNetwork=no
RestrictAddressFamilies=AF_UNIX AF_NETLINK
```

That is the entire widening, and `systemd-analyze security` moved from 1.3 to
1.8. `IPAddressDeny=any` and a new `SocketBindDeny=any` keep the helper unable
to *use* the network it can now see, and the security gate asserts each line
exactly and rejects a second occurrence of any of them.

**It talks to the kernel, not to `ip`.** A process-execution primitive inside a
CAP_NET_ADMIN daemon is one unchecked string from being an arbitrary-command
primitive, and a security test greps for one. The cost is hand-encoded
`RTM_NEWADDR` and `RTM_DELADDR`, and a message parser with explicit bounds
checks rather than the standard library's unsafe pointer casts.

**Guardian never removes an address it did not add.** Every address the adapter
places carries the IPv4 label `<interface>:gdn`. An address without it is the
host's, and both directions are refused with `address-held-by-host`. This is
the rule that stops a misconfigured allowlist becoming a customer outage, and it
is asserted against a live kernel.

The capability is probed rather than assumed: a helper without `CAP_NET_ADMIN`
or without netlink reports `unsupported` with the cause, and the reconciler
turns that into a status that never claims presence.

## 8. Acceptance criteria

1. An address another host answers on is refused, and nothing reaches the host.
2. An address whose freedom cannot be established is refused for that reason,
   distinguishably.
3. A failed apply issues a release in the same pass and holds nothing.
4. An unconfirmed or unsupported apply is never reported as present.
5. A restarted reconciler can release what a previous process applied.
6. An address moving between decoys is released before it is taken.
7. `task check` passes.

All seven are met, and `AC-ON-004` is met end to end: the probe answers
truthfully on a real segment, the reconciler refuses on that answer, and an
address the probe has cleared is now actually placed — `task presence:netlink`
adds one to a live interface, observes it, removes it, and proves both are
idempotent.

## 9. What this package still does not do

**Nothing calls the reconciler yet.** `NewHelperDriver` joins the reconciler to
the helper and is asserted to satisfy the real client's signature, but the Edge
has no source of desired decoy addresses to reconcile against: that arrives with
the decoy desired-state path, not here. The chain is complete and idle rather
than complete and running.

**A decoy address without a listener is still just an address.** `P2-W3` owns
the runtime, and the agreed order puts it before any attacker-facing pack.

**Decoy addresses are `/32` identities, as `ADR 0016` says.** This package
first documented "the prefix length is the zone's" and applied whatever it was
given. A zone-length secondary installs a connected route for the whole subnet,
which on an interface that does not already carry it claims routing for
addresses Guardian does not own. Found while building `P2-W2` and corrected
before anything called the reconciler: `presence.Address` accepts only a `/32`,
and the adapter refuses any other length with `address-must-be-host-identity`
before the kernel is asked.
