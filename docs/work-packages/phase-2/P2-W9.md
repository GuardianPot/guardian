---
id: P2-W9
phase: 2
wave: capability
title: Synthetic credential domain
status: draft
risk: high
components:
  - control-plane
decision_refs:
  - DC-11
  - EV-02
  - EV-03
  - CS-06
acceptance_refs:
  - "P2-W9 acceptance: SSH/PG valid decoy credential creates strong evidence"
  - "P2-W9 acceptance: normal UI does not reveal reusable secret after intended workflow"
depends_on:
  - P2-W15
  - P2-W10
blocks:
  - P2-W5
  - P2-W7
allowed_paths:
  - "apps/control-plane/internal/credential/**"
  - "docs/work-packages/phase-2/**"
  - "security/**"
forbidden_paths:
  - "apps/web-console/**"
  - "0-planning-documents/**"
requires_owner_review: true
requires_security_review: true
---

# P2-W9 — Synthetic credential domain

## 1. Purpose

Plant credentials whose only value is that using one is evidence.

## 2. The inversion, and its one hard limit

Everywhere else in this system a credential is something to protect. Here it is
bait: deliberately discoverable, deliberately worthless, and valuable precisely
because nobody with a legitimate reason would ever type it.

That inverts the handling in one direction only. It is fine for an attacker to
find one. It is not fine for one to work anywhere real, and it is not fine for
Guardian to keep the plaintext after the operator has planted it.

## 3. Scope

`apps/control-plane/internal/credential`: generation, the hash-only stored
form, association with a decoy, the placement workflow, matching, and the
lifecycle `pending → placed → triggered → revoked`.

## 4. The decisions worth arguing about

**The secret exists exactly once.** `New` returns it; nothing stores it. The
`Credential` struct has no plaintext field, so it cannot reach a log, a query
result, or a console by accident. The acceptance criterion — the UI must not
reveal a reusable secret after the intended workflow — is therefore a property
of the type rather than of the screen rendering it.

**Every secret is marked `gdn-decoy-`.** A deliberate trade, recorded in full
in the package comment and the security review. It tells an attacker who has
found one that they are in a honeypot; it also tells anyone finding one in a
paste, a log, or a production password manager that it grants nothing. The
second matters more, because it arrives *before* the credential is typed and the
tell arrives after — by which point the evidence already exists.

**A credential nobody planted is not live.** `pending` sits between minting and
the operator confirming placement. Counting an unplanted credential as live
would overstate coverage, which is the failure this product exists not to make.

**Triggering is idempotent.** An attacker returning to a decoy reports the same
credential again; the first use is what an investigator needs. Overwriting the
timestamp would move "first seen" forward on every visit.

**Revoking stops new evidence and erases none.** `CS-06` forbids silent
deletion: a credential somebody used stays one somebody used.

**Strict base64.** Found by a test. A lenient decoder accepts several textual
spellings of the same 16 bytes, because the final character of a 22-character
encoding carries spare bits. One credential must have exactly one spelling, or
"the attacker typed the value we planted" stops being a precise claim.

## 5. Delivery to the decoy — decided, not implemented

**The question.** A decoy has to compare what an attacker typed against the
planted hash, and `P2-W15` deliberately gave the decoy desired object nowhere to
put a credential — no field exists in the API, on the wire, or in the database.
A desired-state field or a separate fetch would each change `openapi/` or
`proto/`.

**The decision (Product Owner, 2026-09-13): the workload definition.** The
recognition material travels the way a decoy's address already does since
`P2-W3` (ADR 0019): in the root-installed workload definition at
`/etc/guardian-edge/workloads/<workload-id>.json`. No public contract changes.
Placement stays manual, as the roadmap specifies for the pilot.

**The shape to implement.**

- The Control Plane domain renders, for a placed credential, the entry an
  operator adds to the definition. No plaintext: `credential_id` (UUIDv7),
  `kind` (the closed `Kind` set), `username`, and `secret_sha256` (lowercase hex
  of the existing `SecretHash`). It is rendered while the credential is `placed`
  and not after it is revoked.
- The workload definition gains an optional `synthetic_credentials` array of
  those entries, strictly decoded like every other field: unknown fields
  refused, a bounded count, no duplicate `credential_id`, kinds from the closed
  set, username bounded, hash exactly 64 lowercase hex characters.
- An Edge package shared by the privileged helper (validation) and the
  unprivileged Edge Agent (recognition) holds the entry type and the
  recognition rule, which must be byte-for-byte the Control Plane's: the
  `gdn-decoy-` prefix, strict unpadded URL base64 of exactly 16 bytes, SHA-256,
  and a constant-time comparison against every entry of the matching kind,
  without an early exit.
- A match populates `auth.synthetic_credential_id` in the `P2-W10` envelope. The
  typed value never enters an event, a log, or a quarantine record.
- Tests in both modules pin the same fixtures (one secret, its hash, its entry),
  so the two implementations cannot drift apart.
- Revocation is removing the entry from the definition; the Control Plane
  record keeps the trigger history (`CS-06`).

**What stays open.** Where the Edge Agent reads the definition from, and how a
pack's adapter reaches the recognizer, belong to `P2-W5` and `P2-W7`, which are
the first packs that present credentials. Storage, the REST surface, and the
console placement screen follow this decision and are not yet written; the
console must show the one-time secret exactly once, and the placement note must
not be rendered beside decoy content.

## 6. Non-goals

- **No pack integration.** `P2-W5` (SSH) and `P2-W7` (PostgreSQL) present the
  credentials and recognise them; this defines what one is.
- **No storage.** No table and no migration yet; the shape is settled first
  because the delivery decision in section 5 may change what is stored.
- **No automatic placement.** The roadmap says manual pilot placement, and an
  automated planter that wrote into real systems would be the single most
  dangerous component in the product.
