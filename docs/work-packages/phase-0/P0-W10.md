# P0-W10 — Establish versioned canonical device and telemetry contract skeleton

- Status: IN PROGRESS
- Risk: High
- Dependency: P0-W1 plus relevant spike evidence
- Primary component: Contracts
- Owner: `@sinanganiz`

## Scope

Versioned device and telemetry contract skeleton. Do not over-model Phase 1–5
features before spike evidence is available.

## Acceptance evidence

- versioning and compatibility rules;
- contract validation fixture;
- traceability to approved architecture and MVP criteria;
- owner review for `/proto/**`, `/openapi/**`, and `/schemas/**`.

## Execution evidence

- Canonical contract rules: `docs/contracts/README.md` and ADR 0017.
- Protobuf skeleton: `proto/guardian/device/v1/` and
  `proto/guardian/telemetry/v1/`.
- JSON Schema skeleton: `schemas/device/v1/` and `schemas/telemetry/v1/`.
- OpenAPI surface: `/v1/telemetry` in `openapi/guardian.yaml`.
- Validation fixture: `tools/check-canonical-contracts.mjs`.
