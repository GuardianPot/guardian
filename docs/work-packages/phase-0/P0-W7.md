# P0-W7 — Validate Cowrie SSH medium-interaction adapter

- Status: ACCEPTED FOR PHASE 0 FIXTURE SCOPE
- Risk: High
- Dependency: P0-W6
- Primary component: Decoy/SSH
- Owner: `@sinanganiz`

## Scope

Pinned upstream provenance, login/session/command canonical fixture, hostile
input handling, egress test, and license/security notes.

## Acceptance evidence

- canonical session evidence fixture;
- hostile-input and egress tests;
- provenance and license record;
- security review.

The dependency on P0-W6 is resolved. Evidence is implemented by
`tools/cowrie-fixture.ps1`, `tools/cowrie-fixture.sh`,
`tools/check-cowrie-events.mjs`, `deploy/cowrie/PROVENANCE.md`,
`deploy/cowrie/README.md`, and `security/p0-w7-cowrie-review.md`.

## P0-W7-CI1 approved maintenance correction

- Status: APPROVED FOR IMPLEMENTATION
- Owner authorization: 2026-08-28
- Tracking issue: #48
- Incident evidence: post-merge workflow run `33146580026`, where attempt 1
  panicked while concurrent SSH stdout and stderr copies wrote to one
  `bytes.Buffer`; attempt 2 passed and confirmed the failure was intermittent.

The correction is limited to independent stdout and stderr buffers in the
disposable fixture client, a concurrent Go regression test executed with the
race detector on Linux, repeated successful interactive fixture sessions, and
the corresponding security evidence update.

Allowed paths:

- `docs/work-packages/phase-0/P0-W7.md`
- `security/p0-w7-cowrie-review.md`
- `tools/cowrie-client/main.go`
- `tools/cowrie-client/main_test.go`
- `tools/cowrie-fixture.sh`

Acceptance requires that stdout and stderr never share a mutable buffer, the
concurrent regression test and Linux race detector pass, repeated fixture
sessions produce valid canonical evidence, and all required CI checks pass.
Owner/security acceptance remains required before merge. This maintenance
correction does not authorize changes to product scope, architecture, trust
boundaries, privileges, secrets, production behavior, or release authority.
