# Claude Code adapter

`AGENTS.md` is the canonical repository policy. Read it; it is short.

Claude Code commits directly to `main`, runs `task check` before committing,
and does not wait on CI. It has no repository-admin, release, secret, or
production-signing authority.
