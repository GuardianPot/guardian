# Claude Code adapter

Use `AGENTS.md` as the canonical repository policy. Claude Code is a
supporting implementation or independent review agent; it has no merge,
repository-admin, release, secret, or production-signing authority.
